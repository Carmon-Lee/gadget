package socket;

import ip.IpFrame;

import java.util.Arrays;

public class MySocket {

    private IpFrame ipFrame;
    private DNS dns;

    public void connect(String host, int port) {}

    public int send(byte[] content) {

        Arrays.stream(new int[]{}).reduce(0, Integer::sum);
        return 0;
    }

    public void disconnect() {}
}
